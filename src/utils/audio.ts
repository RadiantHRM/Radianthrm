export const sounds = {
  play: (soundName: string) => {
    // Basic implementation to satisfy imports
    console.log(`Playing sound: ${soundName}`);
    // In a real app, you'd use the Web Audio API or an <audio> element
  }
};
